1. controller需要确保存在的import

2. 没有返回值时，handler方法体的格式（使用%s占位符代替调用service的表达式）

3. 有返回值时，handler方法体的格式（使用%s占位符代替调用service的表达式）

4. 没有返回值时，handler方法签名上的返回值

5. 有返回值时，handler方法签名上的返回值

6. handler方法签名上需要确保存在的注解







1. 遍历所有的controller

2. 遍历所有controller下的initializerDeclaration

3.