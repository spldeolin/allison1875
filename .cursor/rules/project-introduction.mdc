# Allison1875 项目 - Cursor AI 编码助手指南

## 项目简介

Allison1875 是基于 **Java AST 技术**的源码生成工具集，Maven 插件形式，专为 **Spring Boot + MyBatis** 开发者设计。通过编写轻量级 Java 内部 DSL，自动生成 Controller、Service、DTO、Mapper、XML 等样板代码。

- 🔗 **GitHub**: https://github.com/spldeolin/allison1875
- 📦 **GroupId**: `com.spldeolin.allison1875`
- 🏷️ **版本**: 12.2 (正式版) / 13.0-SNAPSHOT (开发版)
- 🎯 **目标**: 消除重复编码，提升开发效率

## 核心功能 (5个 Maven Goal)

| Goal | 功能 | 触发场景 |
|------|------|----------|
| `mvn allison1875:persistence-generator` | 数据库表 → 持久层代码 | 新建表、表结构变更 |
| `mvn allison1875:handler-transformer` | DSL → HTTP API | 创建接口、添加 handler |
| `mvn allison1875:query-transformer` | 链式 DSL → 条件查询 | 动态条件、复杂查询 |
| `mvn allison1875:star-transformer` | 星型 DSL → 关联查询 | 一对一、一对多关联 |
| `mvn allison1875:doc-analyzer` | Controller → API 文档 | 文档生成、同步 YApi |

## 项目架构 (Maven 多模块)

```
📁 allison1875/
├── 📦 common/                          # 公共工具 (AST、配置、日志)
├── 📦 allison1875-support/             # 用户运行时依赖 (@L, @P 注解)
├── 📦 allison1875-maven-plugin/        # Maven Mojo 入口
├── 📦 persistence-generator/           # 功能1: DB → Entity/Mapper
├── 📦 persistence-generator-facade/    # persistence-generator 门面
├── 📦 handler-transformer/             # 功能2: DSL → Controller/Service
├── 📦 query-transformer/               # 功能3: 链式 → 条件查询
├── 📦 star-transformer/                # 功能4: 星型 → 关联查询
└── 📦 doc-analyzer/                    # 功能5: Controller → 文档
```

## DSL 语法速查

### 1. Handler Transformer DSL (实例初始化块)
**关键**: 必须放在 `@RestController` 类的 `{ ... }` 实例初始化块中

```java
@RestController
@RequestMapping("/api")
public class UserController {
    {  // ← 实例初始化块 (非静态)
        String handler = "createUser", desc = "创建用户";

        class req {
            @NotNull String userName;
            @Email String email;
            Integer age = 18;  // 默认值

            class address {    // 嵌套对象
                String province;
                String city;
            }
        }

        @P  // 分页包装 (PageResult<CreateUserResp>)
        class resp {
            Long userId;
            String userName;

            @L  // List 类型
            class roles {
                String roleName;
                String roleCode;
            }
        }
    }
}
```

### 2. Query Transformer DSL (链式调用)
**前置**: 需要 Design 类 (persistence-generator 生成)

```java
// 在 Service 实现类中编写
List<User> users = UserDesign
    .query("findActiveUsers")        // Mapper 方法名
    .id.userName.email.createTime    // SELECT 字段 (可选)
    .where()                         // WHERE 开始
    .userName.like(keyword)          // LIKE '%keyword%' (自动 null 检查)
    .status.eq(1)                    // = 1
    .createTime.ge(startDate)        // >= startDate
    .age.between(18, 65)             // BETWEEN 18 AND 65
    .list();                         // 终结: List<User>

// 其他终结操作
Map<String, List<User>> grouped = UserDesign.query("groupByCity")
    .where().status.eq(1)
    .groupByCity();                  // GROUP BY city

Long count = UserDesign.query("countActive")
    .where().status.eq(1)
    .count();                        // COUNT(*)

User single = UserDesign.query("findById")
    .where().id.eq(userId)
    .one();                          // 单个对象或 null
```

### 3. Star Transformer DSL (星型关联)
**前置**: 所有相关表需要 Design 类

```java
// 中心实体 + 关联实体一次查询
OrderWholeDTO orderDetail = StarSchema
    .cft(OrderDesign.orderId, targetOrderId)    // 中心事实表 (Central Fact Table)
    .oo(OrderDetailDesign.orderId)              // 一对一 (One-to-One)
    .om(OrderItemDesign.orderId)                // 一对多 (One-to-Many)
    .om(OrderLogDesign.orderId)                 // 多个一对多
    .over();                                    // 结束

// 生成结果: OrderWholeDTO
// {
//   Order order;                    // 中心实体
//   OrderDetail orderDetail;        // 一对一
//   List<OrderItem> orderItems;     // 一对多
//   List<OrderLog> orderLogs;       // 一对多
// }
```

## 必需配置模板

### pom.xml 完整配置

```xml
<build>
    <plugins>
        <plugin>
            <groupId>com.spldeolin.allison1875</groupId>
            <artifactId>allison1875-maven-plugin</artifactId>
            <version>12.2</version>
            <configuration>
                <!-- 通用配置 -->
                <common>
                    <basePackage>com.example.yourapp</basePackage>
                    <author>yourname</author>
                    <!-- 可选: 覆盖默认包路径约定 -->
                    <entityPackage>com.example.yourapp.entity</entityPackage>
                    <mapperPackage>com.example.yourapp.mapper</mapperPackage>
                    <designPackage>com.example.yourapp.design</designPackage>
                    <servicePackage>com.example.yourapp.service</servicePackage>
                    <reqDTOPackage>com.example.yourapp.dto.req</reqDTOPackage>
                    <respDTOPackage>com.example.yourapp.dto.resp</respDTOPackage>
                    <enableJavaxMoveToJakarta>false</enableJavaxMoveToJakarta>
                </common>

                <!-- 数据库持久层配置 -->
                <persistenceGenerator>
                    <jdbcUrl>jdbc:mysql://localhost:3306</jdbcUrl>
                    <userName>root</userName>
                    <password>root123</password>
                    <schema>your_database</schema>
                    <tables>*</tables>  <!-- 所有表 或 user,order,product -->
                    <enableGenerateDesign>true</enableGenerateDesign>
                    <isEntityEndWithEntity>false</isEntityEndWithEntity>
                    <deletedSql>is_deleted = 1</deletedSql>
                    <notDeletedSql>is_deleted = 0</notDeletedSql>
                </persistenceGenerator>

                <!-- 接口生成配置 -->
                <handlerTransformer>
                    <pageTypeQualifier>com.example.common.Result</pageTypeQualifier>
                    <enableOneService>false</enableOneService>
                </handlerTransformer>

                <!-- 文档生成配置 -->
                <docAnalyzer>
                    <flushTo>MARKDOWN</flushTo>
                    <markdownDir>docs/api</markdownDir>
                    <enableCurl>true</enableCurl>
                    <enableResponseBodySample>true</enableResponseBodySample>
                    <!-- YApi 配置
                    <flushTo>YAPI</flushTo>
                    <yapiUrl>http://yapi.company.com</yapiUrl>
                    <yapiToken>your-token</yapiToken>
                    -->
                </docAnalyzer>
            </configuration>
        </plugin>
    </plugins>
</build>

<dependencies>
    <!-- Allison1875 运行时支持 -->
    <dependency>
        <groupId>com.spldeolin.allison1875</groupId>
        <artifactId>allison1875-support</artifactId>
        <version>12.2</version>
    </dependency>
</dependencies>
```

## 开发工作流

### 初次使用 (一次性设置)
```bash
# 1. 克隆并本地安装 (Allison1875 未发布到 Maven Central)
git clone https://github.com/spldeolin/allison1875.git
cd allison1875
mvn install -DskipTests

# 2. 回到用户项目，配置 pom.xml (参考上面模板)

# 3. 从数据库生成基础持久层代码
mvn allison1875:persistence-generator
```

### 日常开发流程
```bash
# 1. 在 Controller 中编写接口 DSL (实例初始化块)
# 2. 执行转换
mvn allison1875:handler-transformer

# 3. 在 Service 中编写查询 DSL (可选)
# 4. 执行转换
mvn allison1875:query-transformer

# 5. 编写关联查询 DSL (可选)
mvn allison1875:star-transformer

# 6. 生成 API 文档 (可选)
mvn allison1875:doc-analyzer
```

### 测试和构建
```bash
# 编译项目
mvn compile

# 运行测试
mvn test
mvn test -Dtest=UserServiceTest

# 安装到本地仓库
mvn install -DskipTests
```

## 常见错误和解决方案

| 🚨 错误信息 | 🔍 原因分析 | ✅ 解决方案 |
|------------|------------|------------|
| `Plugin 'com.spldeolin.allison1875:allison1875-maven-plugin' not found` | 未本地安装 Allison1875 | `git clone` 项目并 `mvn install` |
| handler-transformer 执行但没有生成代码 | DSL 语法错误或位置错误 | 确保 DSL 在 `@RestController` 类的 `{ }` 实例初始化块中 |
| `XxxDesign cannot be resolved` | 未生成 Design 类 | 先运行 `persistence-generator` 并开启 `enableGenerateDesign=true` |
| 生成的代码使用 `javax.*` 但项目需要 `jakarta.*` | Spring Boot 3.x 版本差异 | 设置 `<enableJavaxMoveToJakarta>true</enableJavaxMoveToJakarta>` |
| 数据库连接失败 | 数据库配置错误 | 检查 `jdbcUrl`、`userName`、`password`、`schema` 配置 |
| 一个 Controller 中多个 DSL 只生成了一个接口 | DSL 语法冲突 | 每个 `{ }` 块独立定义一个 handler，检查变量名冲突 |

## 核心包名约定

### 生成代码的默认包结构
```
com.example.yourapp/
├── entity/          # 实体类 (XxxEntity.java 或 Xxx.java)
├── mapper/          # Mapper 接口 (XxxMapper.java)
│   └── param/       # 查询参数 DTO (XxxParam.java)
├── design/          # Design 查询入口 (XxxDesign.java)
├── service/         # Service 接口 (XxxService.java)
│   └── impl/        # Service 实现 (XxxServiceImpl.java)
├── dto/
│   ├── req/         # 请求 DTO (XxxReq.java)
│   └── resp/        # 响应 DTO (XxxResp.java)
└── controller/      # Controller (手动创建)
```

### MyBatis XML 文件位置
```
src/main/resources/
└── mapper/          # MyBatis XML 文件 (XxxMapper.xml)
```

## 注解和约定

### Allison1875 专用注解
```java
import com.spldeolin.allison1875.support.L;  // List 类型标记
import com.spldeolin.allison1875.support.P;  // Page 分页标记

// 在 handler-transformer DSL 中使用
@P class resp { ... }     // 响应会被 PageResult<RespDTO> 包装
@L class items { ... }    // 字段类型为 List<ItemsDTO>
```

### JSR-303 验证注解 (保留)
```java
// 这些注解会被保留到生成的 DTO 中
@NotNull
@NotBlank
@NotEmpty
@Email
@Size(min = 1, max = 100)
@Pattern(regexp = "^[0-9]+$")
@Valid     // 嵌套对象验证
```

## 性能优化建议

### 1. Design 类缓存
```java
// 推荐: 缓存 Design 实例，避免重复创建
private static final UserDesign USER_DESIGN = UserDesign.getInstance();

List<User> users = USER_DESIGN
    .query("findByKeyword")
    .where().userName.like(keyword)
    .list();
```

### 2. 批量操作
```java
// 使用 MyBatis 批量插入而不是循环单条插入
@Mapper
public interface UserMapper {
    // persistence-generator 会生成基础的 insertBatch 方法
    int insertBatch(List<User> users);
}
```

### 3. 分页查询
```java
// 使用 @P 注解自动包装分页结果
{
    String handler = "listUsers";
    class req {
        String keyword;
        int pageNum = 1;
        int pageSize = 20;
    }

    @P class resp {
        Long userId;
        String userName;
    }
}
```

## 调试技巧

### 查看生成的代码
```bash
# 编译后查看生成的字节码对应的源码
javap -c -p target/classes/com/example/UserController.class

# 或直接查看生成的 DTO 源文件
find . -name "*Req.java" -o -name "*Resp.java" | head -10
```

### 开启详细日志
在 `src/main/resources/logback.xml` 中添加:
```xml
<logger name="com.spldeolin.allison1875" level="DEBUG"/>
```

### 验证 DSL 语法
```java
// 在 DSL 转换前，可以添加编译期验证
public void validateDSL() {
    // 确保实例初始化块语法正确
    String handler = "testHandler";  // ✅ 正确
    // String h = "testHandler";     // ✅ 也支持简写
    // static String handler = "";   // ❌ 不能是静态

    class req {
        String field1;               // ✅ 基本类型
        @NotNull String field2;      // ✅ 注解会保留
        int field3 = 10;            // ✅ 默认值会保留
        List<String> field4;        // ❌ 避免直接使用 List，用 @L 嵌套类
    }
}
```

---
**📝 提示**: 这个项目专为减少 Spring Boot + MyBatis 项目的样板代码而生。合理使用可以显著提升开发效率！

**🔄 更新**: 2025-02 | 版本: 12.2/13.0-SNAPSHOT